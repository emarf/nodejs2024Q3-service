export enum DB_Field {
  ARTISTS = 'artists',
  ALBUMS = 'albums',
  TRACKS = 'tracks',
  USERS = 'users',
  FAVORITES_TRACKS = 'favoritesTracks',
  FAVORITES_ALBUMS = 'favoritesAlbums',
  FAVORITES_ARTISTS = 'favoritesArtists',
}
